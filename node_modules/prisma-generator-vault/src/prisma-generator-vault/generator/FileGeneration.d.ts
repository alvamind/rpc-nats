export type FileGenerationOptions = {
    outputPath: string;
};
export declare abstract class FileGeneration {
    protected outputPath: string;
    static fileName: string;
    constructor({ outputPath }: FileGenerationOptions);
    abstract generate(): string[];
    protected abstract get fileName(): string;
    protected writeFile(contents: string): string;
}
//# sourceMappingURL=FileGeneration.d.ts.map